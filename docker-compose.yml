version: "3.9"
services:
  app:
    image: node:20
    container_name: portfolio
    working_dir: /app
    volumes:
      - portfolio_data:/app
    ports:
      - "4000:3000"
    env_file:
      - .env
    command: sh -c "node server.js"
    
volumes:
  portfolio_data:
